// import { axiosInstance } from "../Utility/axiosInstance";

// // url, type, data,
// export const usePromise = (basicData, thens, catches) => {
//   let chain = axiosInstance[basicData.type](basicData.url, basicData.data);

//   // looping .thens
//   thens.foreach((el) => (chain = chain.then(el)));

//   // looping .catches
//   catches.foreach((el) => (chain = chain.catch(el)));
// };
